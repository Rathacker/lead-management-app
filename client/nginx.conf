server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths resolve to the app shell so client routing works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
